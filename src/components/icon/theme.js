import defaultTheme from '../themes';

const iconTheme = {
  size: {
    lg: 24,
    md: 20,
    sm: 16,
  },
  paddingSize: {
    lg: 12,
    md: 10,
    sm: 8,
  },
  color: defaultTheme.colors,
};

export default iconTheme;
