import defaultTheme from '../themes';

const theme = {
  color: defaultTheme.colors.riverBedDark,
  sizes: {
    large: 24,
    largeMargin: 12,
    medium: 20,
    mediumMargin: 10,
    small: 16,
    smallMargin: 8,
  },
};

export default theme;
