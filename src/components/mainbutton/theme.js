import defaultTheme from '../themes';

const buttonTheme = {
  size: {
    lg: {
      padding: '10px 48px',
      font: 20,
      minWidth: 172,
    },
    md: {
      padding: '10px 32px',
      font: 14,
      minWidth: 128,
    },
    sm: {
      padding: '8px 16px',
      font: 10,
      minWidth: 80,
    },
  },
  colors: defaultTheme.colors,
};

export default buttonTheme;
