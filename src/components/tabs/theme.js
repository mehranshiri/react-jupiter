import defaultTheme from '../themes';

const theme = {
  defaultColor: defaultTheme.colors.blue400,
  secoundColor: defaultTheme.colors.blue600,
  borderRadius: defaultTheme.borderRadius.high,
  transition: `all ${defaultTheme.animation.time}s ${defaultTheme.animation.method}`,
};

export default theme;
